{% extends 'base.html.twig' %}

{% block title %}Editer un message{% endblock %}

{% block body %}

    <div class="d-flex justify-content-center mt-3">
        <h2 class="text-center mt-3">Editer un message</h2>
    </div>

<div class="container">

    {{ include('messagerie/_form.html.twig', {'button_label': 'Mettre à jour'}) }}


    {{ include('messagerie/_delete_form.html.twig') }}

<a class="btn btn-light" href="{{ path('app_messagerie_index') }}">Retour à la liste</a>
</div>

{% endblock %}